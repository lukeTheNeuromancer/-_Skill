# 理解注意力机制

### 从 Q、K、V 到现代 Transformer 注意力

一本技术手册，讲解注意力机制究竟在计算什么、为什么公式具有这样的特定形式，以及现代大语言模型系统如何执行它。



---

## 本手册的目标

读完本手册后，你应当能够看着这个公式

$$
\text{Attention}(Q, K, V) = \text{softmax}\left(\frac{QK^\top}{\sqrt{d_k}}\right)V
$$

并解释其中每一个对象和每一步运算：Q、K、V 从何而来；$QK^\top$ 中每个元素的含义；为什么要对分数进行缩放；掩码如何改变允许的连接方式；为什么 softmax 是按行计算的；以及为什么与 V 相乘会为每个查询位置生成一个新的表示。

你还应当能够解释同样的核心机制是如何出现在自注意力、交叉注意力、因果语言模型、多头注意力、KV 缓存解码、MQA、GQA、MLA、FlashAttention 以及稀疏/局部注意力中的。

### 前置知识

你需要对向量、矩阵、矩阵乘法、点积、softmax 以及神经网络线性层有基本的理解。本书不假设你已具备 Transformer 内部结构的先验知识。

### 范围

本书是一本关于注意力机制的手册。书中涉及 RoPE、KV 缓存、MQA/GQA/MLA 以及 FlashAttention，但仅限于解释它们如何修改、支撑或执行注意力运算。完整的推理服务、PagedAttention、长上下文训练以及模型并行则值得另写专书。

### 准确性说明

书中提到的具名方法均以其原始论文为依据；基础性讲解已与当前的斯坦福课程材料进行交叉核对；实现相关的行为已与当前的 PyTorch 文档核对。关于历史论文的论断均严格限定在这些论文实际确立的结论范围内。[1, 2, 3, 15, 16, 17]

---

## 目录

1. Transformer 出现之前：注意力机制为何诞生
2. 注意力：一种依赖内容的加权聚合
3. 记号与张量形状
4. Q、K、V 从何而来
5. 分数矩阵：$QK^\top$
6. 为什么要除以 $\sqrt{d_k}$？
7. 掩码：哪些连接是被允许的？
8. Softmax 与值的聚合
9. 手算示例一：Q、K、V 与原始分数
10. 手算示例二：softmax 权重
11. 手算示例三：输出向量
12. 自注意力 vs 交叉注意力
13. 多头注意力
14. 位置信息：注意力需要一个顺序信号
15. 自回归语言模型中的因果注意力
16. 训练与解码
17. KV 缓存
18. MHA、MQA 与 GQA
19. MLA：压缩 KV 表示
20. 究竟什么是"二次方"的？
21. FlashAttention：相同的注意力，不同的执行方式
22. FlashAttention 的迭代：优化你真正遇到的瓶颈
23. 稀疏注意力与局部注意力
24. 一个透明的 PyTorch 实现
25. PyTorch 中的生产级 SDPA
26. 常见的实现错误
27. 常见的误解
28. 注意力权重是解释吗？
29. 完整的心智模型

参考文献与延伸阅读

---

## 1 Transformer 出现之前：注意力机制为何诞生

早期的神经序列到序列系统通常将整个源序列编码为一个固定长度的向量，然后要求解码器根据这个表示生成目标序列。Bahdanau、Cho 和 Bengio 指出，这种固定向量的接口可能成为一个瓶颈，尤其是随着源序列变长时更是如此。他们的模型让解码器在每一个输出步骤都能够形成不同的上下文向量，方法是根据编码器状态与当前解码状态的相关性对其进行软加权。[1]

当时的关键思想还不是"自注意力"。解码器使用的是一种学习到的兼容性机制，用来决定哪些源表示应当更强烈地作用于当前输出。Luong、Pham 和 Manning 后来研究了多种神经机器翻译中的注意力变体，包括对全部源位置的全局注意力和对源位置子集的局部注意力。[2]

2017 年的 Transformer 则更为激进地改变了注意力的角色。它不再以循环或卷积作为在序列中传递信息的主要机制，而是围绕注意力加上逐位置的前馈层来构建其编码器和解码器。该论文将自注意力描述为：关联单一序列中不同位置以计算新表示的机制。[3]

**心智模型。** 注意力并非始于"Transformer 公式"。其持久的核心思想是**依赖内容的学习型聚合**：一个查询决定不同的键值对以多强的程度参与到输出中。Transformer 的贡献在于把某种特定的注意力形式变成了核心的序列混合运算。

**本书将要研究的内容**

本书余下部分聚焦于缩放点积注意力，因为这正是 Transformer 以及现代大语言模型注意力内核所使用的形式。早期的加性注意力机制在历史上很重要，但它们的打分函数与此不同。

---

## 2 注意力：一种依赖内容的加权聚合

注意力函数可以被描述为：将一个查询和一组键值对映射为一个输出。每个键都被用来判断其对应的值与该查询的兼容程度；输出则是各个值的加权组合。这一通用描述直接出现在 Transformer 论文中。[3]

对于单个查询向量 $q$ 和键值对 $(k_j, v_j)$，缩放点积注意力可以写作：

$$
s_j = \frac{q^\top k_j}{\sqrt{d_k}}, \qquad a_j = \frac{e^{s_j}}{\sum_r e^{s_r}}, \qquad o = \sum_j a_j v_j.
$$

分数 $s_j$ 之所以是一种学习到的兼容性信号，是因为 $q$ 和 $k_j$ 本身都来自学习到的投影。Softmax 将这些分数转化为在所有被允许的键上求和为一的非负权重。输出 $o$ 于是就是值空间中的一个加权平均。

这种表述比"查询提出问题、键包含答案"之类的常见拟人化说法更为精确。这些类比虽然容易记住，但并不是真正的机制。查询和键都只是学习到的表示空间中的向量，它们的点积给出的是一个标量兼容性分数。

**心智模型。** 键决定权重，值提供被混合的内容。这种职责分离正是注意力同时需要 K 和 V 的概念性原因。

**一种有用的抽象**

对于一次性处理多个查询，注意力就变成了矩阵计算：

$$
S = QK^\top, \qquad A = \text{softmax}(S/\sqrt{d_k}), \qquad O = AV.
$$

后续章节将逐一拆解每个对象及其形状。

---

## 3 记号与张量形状

约定：

- $B$：批大小（batch size）
- $L$：查询位置数
- $S$：键/值位置数
- $H_q$：查询头数
- $H_{kv}$：键/值头数
- $d_h$：每个头的查询/键维度
- $d_v$：每个头的值维度

对单个头、单个样本而言，可以忽略批次/头维度，写作：

$$
Q \in \mathbb{R}^{L \times d_h}, \quad K \in \mathbb{R}^{S \times d_h}, \quad V \in \mathbb{R}^{S \times d_v}.
$$

于是

$$
QK^\top \in \mathbb{R}^{L \times S}, \quad A \in \mathbb{R}^{L \times S}, \quad AV \in \mathbb{R}^{L \times d_v}.
$$

有两个细节值得注意。

第一，注意力矩阵并不总是方阵。在对单一完整序列的自注意力中，$L = S$。而在交叉注意力中，或者在缓存解码时（一个较短的查询块要关注一个更长的前缀），$L$ 与 $S$ 可以不同。当前的 PyTorch SDPA 文档明确地使用了独立的查询长度 $L$ 与源长度 $S$。[17]

第二，现代的分组注意力可以有 $H_q \neq H_{kv}$。标准的 MHA 通常查询头数与 KV 头数相同；MQA 只使用一个 KV 头；GQA 则使用介于两者之间的头数。[4, 9]

> **重要提示。** 不要不加区分地认为 $n \times n$ 是注意力权重矩阵的通用形状。这在等长自注意力中是正确的，但会掩盖交叉注意力和增量推理的情况。

---

## 4 Q、K、V 从何而来

假设某个注意力层的输入是一组词元表示构成的矩阵：

$$
X \in \mathbb{R}^{n \times d_{model}}.
$$

一个标准的自注意力层会应用学习到的线性投影：

$$
Q = XW_Q, \quad K = XW_K, \quad V = XW_V.
$$

对单个头而言：

$$
W_Q, W_K \in \mathbb{R}^{d_{model} \times d_h}, \qquad W_V \in \mathbb{R}^{d_{model} \times d_v}.
$$

在多头注意力中，每个头在概念上拥有各自不同的学习投影，或者由某个实现计算出一个更大的投影再重塑（reshape）为多个头。Transformer 论文将多头注意力定义为每个头分别使用独立的学习投影。[3]

**流程：** $X \rightarrow Q = XW_Q,\ K = XW_K,\ V = XW_V$

因此，Q、K、V 并不是绑定在词元上的固定语义角色，而是当前隐藏表示的学习视图。同一个词元在不同层、不同头中可以拥有不同的查询/键/值向量。

**心智模型。** 模型学习了两个用于判断兼容性的空间（Q 和 K），以及一个用于承载将被聚合信息的空间（V）。

---

## 5 分数矩阵：$QK^\top$

取查询行 $q_i$ 与键行 $k_j$，它们的点积为：

$$
S_{ij} = q_i^\top k_j.
$$

完整的矩阵乘法 $QK^\top$ 并行计算出每一个查询-键点积：

$$
QK^\top =
\begin{pmatrix}
q_1^\top k_1 & q_1^\top k_2 & \cdots & q_1^\top k_S \\
q_2^\top k_1 & q_2^\top k_2 & \cdots & q_2^\top k_S \\
\vdots & \vdots & \ddots & \vdots \\
q_L^\top k_1 & q_L^\top k_2 & \cdots & q_L^\top k_S
\end{pmatrix}.
$$

每一行对应一个查询位置，每一列对应一个键位置。

这个点积并不是一个概率，而是一个未归一化的兼容性分数。它可正可负，其量级取决于向量的尺度和维度。

矩阵大小为 $L \times S$，条目 $q_i^\top k_j$ 表示第 $i$ 个查询与第 $j$ 个键之间的分数。

> **重要提示。** 把这个分数称作"相似度"虽然方便，但并不完整。点积注意力学习投影的目的恰恰是让得到的兼容性对当前任务有用，它并不需要表现得像一个通用的语义相似度度量。

---

## 6 为什么要除以 $\sqrt{d_k}$？

Transformer 在 softmax 之前对查询-键点积进行了缩放：

$$
\tilde{S} = \frac{QK^\top}{\sqrt{d_k}}.
$$

论文给出了具体的动机。假设 $q$ 和 $k$ 的各分量是均值为零、方差为一的独立随机变量，那么

$$
q^\top k = \sum_{r=1}^{d_k} q_r k_r
$$

的均值为零，方差为 $d_k$。随着 $d_k$ 增大，未经缩放的点积其量级也会随之增长。量级过大的 logits 会把 softmax 推入梯度极小的饱和区间。除以 $\sqrt{d_k}$ 能把这个示意性的方差从 $d_k$ 归一化到大约 1。[3]

在上述假设下，一个简短的推导是：

$$
\text{Var}(q^\top k) = \sum_{r=1}^{d_k} \text{Var}(q_r k_r) = d_k,
$$

因此

$$
\text{Var}\left(\frac{q^\top k}{\sqrt{d_k}}\right) \approx 1.
$$

> **重要提示。** 这个推导是一种动机说明，而不是断言训练好的 Transformer 中学习到的查询/键分量真的始终是独立、零均值、单位方差的随机变量。论文明确指出，这些假设只是用来说明为什么点积会随维度增长这一现象的示意性论证。

**心智模型。** 缩放的目的是让分数的量级保持在一个 softmax 能够随维度增长依然有效工作的范围内，它并不是把 Q 或 K 归一化为单位向量。

---

## 7 掩码：哪些连接是被允许的？

在 softmax 之前，注意力实现可以在分数矩阵上加入掩码或偏置项。对于因果语言建模而言，未来的键位置不能影响更早的查询位置。一种常见的加性掩码是：

$$
M_{ij} =
\begin{cases}
0, & j \le i, \\
-\infty, & j > i.
\end{cases}
$$

于是

$$
A = \text{softmax}\left(\frac{QK^\top}{\sqrt{d_k}} + M\right).
$$

由于 $e^{-\infty} = 0$，被掩盖的条目在 softmax 之后获得的概率为零。

对于长度为 4 的方阵因果注意力矩阵，允许的模式如下（✓ 表示允许，× 表示禁止）：

| | 键1 | 键2 | 键3 | 键4 |
|---|---|---|---|---|
| **查询1** | ✓ | × | × | × |
| **查询2** | ✓ | ✓ | × | × |
| **查询3** | ✓ | ✓ | ✓ | × |
| **查询4** | ✓ | ✓ | ✓ | ✓ |

原始 Transformer 解码器通过把非法的（softmax 前）连接设为 $-\infty$ 来实现自回归掩码。[3]

掩码的用途不止于因果性。填充掩码（padding mask）可以防止对填充词元的关注；结构化注意力可以将连接性限制在局部窗口、块或其他模式之内。当前的 PyTorch SDPA 既接受布尔型掩码也接受加性浮点型掩码，并且单独支持一种因果模式。[17]

> **重要提示。** 在非方阵注意力（$L \neq S$）中，"下三角"这一说法需要一种对齐约定才有意义。当前的 PyTorch 文档明确区分了方阵与非方阵情形下的因果偏置行为。不要在不检查 API 期望行为的情况下，盲目地构造一个 $L \times S$ 的三角矩阵。

---

## 8 Softmax 与值的聚合

在缩放和掩码之后，对每个查询行沿键维度进行 softmax：

$$
A_{ij} = \frac{\exp(\tilde{S}_{ij})}{\sum_{r=1}^{S} \exp(\tilde{S}_{ir})}.
$$

因此，对每个查询 $i$：

$$
A_{ij} \ge 0, \qquad \sum_{j=1}^{S} A_{ij} = 1.
$$

查询位置 $i$ 处的输出为：

$$
o_i = \sum_{j=1}^{S} A_{ij} v_j.
$$

写成矩阵形式：

$$
O = AV.
$$

这最后一次乘法很容易被忽视，但它正是注意力真正移动信息的地方。分数矩阵只是决定每个值贡献多少；而值才是被混合进新表示中的向量。

如果某个查询给三个值分配的权重是 $(0.7, 0.2, 0.1)$，那么它的输出就是：

$$
0.7v_1 + 0.2v_2 + 0.1v_3.
$$

输出仍处于"值向量"空间中，而不是"分数"空间中。

**心智模型。** 分数回答的是"多强？"，值回答的是"聚合什么？"。完整的注意力运算是一个依赖内容的混合算子。

**为什么是按行？**

每个查询会独立地在其被允许的键位置上产生一个分布。按列做 softmax 则会跨查询进行归一化，那将是一种完全不同的运算。

---

## 9 手算示例一：Q、K、V 与原始分数

现在我们完整地手算一个因果注意力头。设

$$
Q = K =
\begin{pmatrix}
1 & 0 \\
0 & 1 \\
1 & 1
\end{pmatrix}, \qquad
V =
\begin{pmatrix}
1 & 0 \\
0 & 2 \\
3 & 1
\end{pmatrix}, \qquad d_k = 2.
$$

共有三个查询位置和三个键/值位置。查询-键点积为：

$$
QK^\top =
\begin{pmatrix}
1 & 0 & 1 \\
0 & 1 & 1 \\
1 & 1 & 2
\end{pmatrix}.
$$

除以 $\sqrt{2}$ 之后：

$$
\frac{QK^\top}{\sqrt{2}} \approx
\begin{pmatrix}
0.7071 & 0 & 0.7071 \\
0 & 0.7071 & 0.7071 \\
0.7071 & 0.7071 & 1.4142
\end{pmatrix}.
$$

现在施加因果掩码。查询 1 只能使用键 1；查询 2 可以使用键 1–2；查询 3 可以使用全部三个键：

$$
\tilde{S}_{causal} \approx
\begin{pmatrix}
0.7071 & -\infty & -\infty \\
0 & 0.7071 & -\infty \\
0.7071 & 0.7071 & 1.4142
\end{pmatrix}.
$$

（本示例中的所有数值在排版前都已通过程序独立复核。）

---

## 10 手算示例二：softmax 权重

逐行施加 softmax。

对第一个查询，只有一个位置是合法的，所以

$$
A_1 = [1, 0, 0].
$$

对第二个查询，合法的 logits 为 $[0, 0.7071]$：

$$
A_2 \approx [0.3302, 0.6698, 0].
$$

对第三个查询，logits 为 $[0.7071, 0.7071, 1.4142]$：

$$
A_3 \approx [0.2483, 0.2483, 0.5035].
$$

因此：

$$
A \approx
\begin{pmatrix}
1 & 0 & 0 \\
0.3302 & 0.6698 & 0 \\
0.2483 & 0.2483 & 0.5035
\end{pmatrix}.
$$

每一行之和都为 1（在舍入误差范围内），并且所有因果上被禁止的位置权重均为零。

注意第三行说明了什么：第三个查询把大约一半的注意力权重分给了第三个值，把大约四分之一分别分给了前两个值。这些数字并不是我们额外添加的注释，而是这个玩具示例中学习到的兼容性计算所归一化出的结果。

> **重要提示。** 在一个训练好的模型中，注意力权重是在多个头和多个层中产生的，并且会与残差连接、归一化、前馈网络以及后续计算相互作用。不应当把一个较高的注意力权重直接当作对模型预测的完整解释。我们会在后文回到这个话题。

---

## 11 手算示例三：输出向量

最后，用注意力权重与值矩阵相乘：

$$
O = AV.
$$

使用

$$
V =
\begin{pmatrix}
1 & 0 \\
0 & 2 \\
3 & 1
\end{pmatrix},
$$

我们得到：

$$
O \approx
\begin{pmatrix}
1 & 0 \\
0.3302 & 1.3395 \\
1.7587 & 1.0000
\end{pmatrix}.
$$

例如，第二行是：

$$
0.3302[1, 0] + 0.6698[0, 2] = [0.3302, 1.3396] \quad (\text{舍入})
$$

第三行是：

$$
0.2483[1, 0] + 0.2483[0, 2] + 0.5035[3, 1] \approx [1.7587, 1.0000].
$$

至此，整个注意力机制已经完全可见：

$$
Q, K \longrightarrow QK^\top \longrightarrow \frac{QK^\top}{\sqrt{d_k}} \longrightarrow \text{掩码} \longrightarrow \text{softmax} \longrightarrow A \longrightarrow AV.
$$

**心智模型。** 如果你能解释清楚这个三词元示例中每一个数字的来龙去脉，那么核心的注意力公式对你来说就不再是一个黑箱。此后的一切内容，改变的都只是 Q/K/V 的来源、允许的连接方式、头的结构、位置信息的处理方式，或者这一计算被高效执行的方式。

---

## 12 自注意力 vs 交叉注意力

注意力公式并不要求 Q、K、V 来自同一个序列。

### 自注意力

在自注意力中，Q、K、V 都是同一序列（或同一层输入）表示的投影：

$$
Q = XW_Q, \quad K = XW_K, \quad V = XW_V.
$$

Transformer 编码器就以这种形式使用自注意力。因果解码器同样使用自注意力，只是加上了对未来位置的掩码。[3]

### 交叉注意力

在编码器-解码器注意力中，查询来自解码器一侧，而键和值来自编码器输出：

$$
Q = YW_Q, \quad K = XW_K, \quad V = XW_V.
$$

如果目标/查询序列长度为 $L$，源序列长度为 $S$，那么分数矩阵的形状为 $L \times S$。

| | Q 的来源 | K、V 的来源 |
|---|---|---|
| 自注意力 | 同一序列 | 同一序列 |
| 交叉注意力 | 查询/目标流 | 源/记忆流 |
| 因果自注意力 | 同一序列 | 同一序列（加掩码）|

> **重要提示。**"自注意力"描述的是 Q/K/V 来自哪里；"因果注意力"描述的是一种连接性约束。两者并不是同义词：自注意力既可以是双向的，也可以是因果的。

---

## 13 多头注意力

单个注意力头只能产生一种学习到的兼容性与聚合模式。多头注意力则使用不同的学习投影并行地执行多个注意力操作，将各个头的输出拼接起来，再施加一次输出投影。Transformer 将其定义为：

$$
\text{MHA}(Q, K, V) = \text{Concat}(\text{head}_1, \dots, \text{head}_H)W_O,
$$

其中

$$
\text{head}_h = \text{Attention}(QW_h^Q,\ KW_h^K,\ VW_h^V).
$$

[3]

**流程：** 输入 → [头1, 头2, ..., 头H]（并行计算） → 拼接 → $W_O$ → 输出

如果 $d_{model}$ 保持固定，且每个头的维度约为 $d_{model}/H$，那么增加头数并不会简单地把全维度注意力的成本乘以 $H$，因为每个头都变窄了。原始 Transformer 正是利用这一设计，使总成本与单个全维度头相当。[3]

> **重要提示。** 不要假设每个头都对应着一个人类可以清晰理解的概念。不同的头确实可以学到不同的模式，但头的语义是涌现出来的，并不保证能映射到简单的标签上。

---

## 14 位置信息：注意力需要一个顺序信号

如果没有任何依赖位置的信号，普通的（非因果）自注意力是"置换等变"的：如果输入的行被重新排列，输出也会以相同方式被重新排列。它本身并没有"某个词元是第三个"或"另一个词元相距五个位置"这样的内在概念。因此，原始 Transformer 在词元嵌入中加入了位置编码。[3]

现代的仅解码器大语言模型常常使用旋转位置编码（RoPE）。RoPE 按照与位置相关的角度旋转查询和键的分量。在一个简化的二维块中：

$$
R_m(\theta) =
\begin{pmatrix}
\cos(m\theta) & -\sin(m\theta) \\
\sin(m\theta) & \cos(m\theta)
\end{pmatrix}.
$$

若 $q_m = R_m q$，$k_n = R_n k$，则

$$
q_m^\top k_n = q^\top R_m^\top R_n k = q^\top R_{n-m} k,
$$

因此注意力的相互作用中包含了相对位移信息。RoFormer 论文将 RoPE 描述为：通过旋转来编码绝对位置，同时在自注意力中引入了显式的相对位置依赖关系。[7]

有两个细微之处值得留意。

第一，位置编码本身并不等同于注意力。它是通过修改表示或 Q/K 之间的交互，使得注意力能够利用位置信息。

第二，因果掩码本身引入了方向性的顺序约束，但它本身并不提供关于距离或绝对位置的丰富表示。

> **重要提示。** RoPE 在数学上能够在超出训练长度的位置上被求值，这并不意味着它能保证可靠的长上下文表现。上下文外推的能力取决于训练过程和位置配置，而不仅仅取决于 RoPE 本身。

---

## 15 自回归语言模型中的因果注意力

一个自回归语言模型将一个词元序列分解为：

$$
p(x_{1:T}) = \prod_{t=1}^{T} p(x_t \mid x_{<t}).
$$

在对一个仅解码器 Transformer 进行完整序列训练时，模型可以并行处理许多位置，因为因果掩码阻止了位置 $t$ 处的表示使用未来的词元。每一层的掩码自注意力都强制执行这种允许的依赖模式。[3]

对于查询位置 $t$，注意力可以使用位置 $1, \dots, t$ 的键/值，但不能使用位置 $t+1, \dots, T$ 的。

这也是训练和生成在计算上感觉如此不同的原因之一。在训练时，完整的已知序列是可用的，大规模的矩阵乘法可以同时处理所有查询位置。而在生成时，词元 $t+1$ 要等到词元 $t$ 已经生成之后才存在，因此解码在被生成的词元之间是串行的。

**心智模型。** 因果掩码使得跨位置的并行训练能够与自回归目标相兼容，但它并不能使自回归生成变成并行的，因为未来生成的词元还是未知的。注意力计算本身仍然是缩放点积注意力；在推理阶段发生变化的，是有多少先前的计算可以被复用。

---

## 16 训练与解码

考虑一个正在处理长度为 $T$ 的前缀的因果解码器层。

### 训练 / 完整前缀处理

对于一个完整的训练序列，或者一次提示词的预填充（prefill），许多查询位置会被一起处理。稠密注意力通常通过大规模的批处理矩阵运算，在相关的查询-键对之间形成交互。

### 增量解码

在前缀被处理完之后，模型会生成一个新词元。在每一层，这个新的隐藏状态都会产生一个新的查询、键和值。关键在于，早先位置的键和值不需要从头重新计算，因为在因果注意力下，早先词元的表示在计算时本就无法访问未来的词元。

这一观察正是 KV 缓存的由来。

| | 预填充/类训练阶段 | 增量解码 |
|---|---|---|
| 处理的查询数量 | 多个位置 | 通常只有一个/几个新位置 |
| 过去的 K/V | 在本次前向传播中计算 | 从缓存中复用 |
| 跨序列并行度 | 高 | 生成过程仍是串行的 |
| 典型瓶颈侧重 | 计算量 + 显存 | 缓存带宽/容量 + 单词元延迟 |

> **重要提示。**"预填充"和"解码"描述的是服务阶段（serving phase），而不是注意力在数学定义上的不同。它们只是为同一个模型暴露出了不同的形状和不同的硬件瓶颈。

---

## 17 KV 缓存

在因果增量解码中，每一层都会存储此前词元产生的键和值向量。当一个新词元到来时，该层计算这个新词元的查询/键/值，将其 K/V 追加到缓存中，并用新的查询去关注被缓存的键和值。

对于标准注意力，每个词元的缓存元素数量大致为：

$$
2 \times L_{layers} \times H_{kv} \times d_h,
$$

其中因子 2 是因为要同时计入 K 和 V。以字节为单位时，再乘以每个元素的存储字节数。

举例来说，假设有 32 层，$d_h = 128$，K/V 元素为 2 字节：

| KV 头数 | 每词元缓存量 | 8192 词元的缓存总量 |
|---|---|---|
| 32 | 512 KiB | 4.0 GiB |
| 8 | 128 KiB | 1.0 GiB |
| 1 | 16 KiB | 0.125 GiB |

这个示例只单独计算了 K/V 的存储量，并未计入分配器开销、元数据、其他激活值、权重以及具体实现相关的表示方式。

KV 缓存避免了在每生成一个词元时都要为整个前缀重复计算 K/V。但它并不能让稠密注意力独立于上下文长度：新的查询仍然要与被缓存的前缀进行交互，缓存所占的内存也会随序列长度增长。

> **重要提示。**"KV 缓存使解码变成 O(1)"这种说法对于标准的稠密注意力而言是错误的。虽然先前的 K/V 投影得到了复用，但新词元的注意力计算量仍会随着被缓存的键/值位置数量增长。

---

## 18 MHA、MQA 与 GQA

KV 缓存使得"键/值头的数量"从一个纯架构细节，变成了一个推理系统层面的决策。

### 多头注意力（MHA）

标准 MHA 中，每个注意力头都有各自独立的 K/V 投影。如果有 $H$ 个查询头，通常也就有 $H$ 个 KV 头。

### 多查询注意力（MQA）

Shazeer 提出 MQA 是为了加快 Transformer 的增量解码速度。它保留了多个查询头，但让它们共享同一个键头和同一个值头，从而大幅缩小被缓存的 K/V 张量以及对内存带宽的需求。[4]

### 分组查询注意力（GQA）

GQA 是对 MQA 的推广，它使用介于两者之间数量的 KV 头：多于一个，但少于查询头的数量。查询头被划分为若干组，每组共享一个 KV 头。Ainslie 等人提出 GQA，作为一种在其实验中能够逼近 MQA 推理效率、同时保持更接近 MHA 质量的方法。[9]

| 机制 | 查询头 | KV 头 |
|---|---|---|
| MHA | 多个 | 与查询头数量相同 |
| GQA | 多个 | 按组共享，数量介于两者之间 |
| MQA | 多个 | 只有一个，被所有查询头共享 |

**心智模型。** MQA/GQA 减少的是 KV 头，而不是查询头。查询头的数量可以保持很高，与此同时被缓存的键/值表示却是共享的。

---

## 19 MLA：压缩 KV 表示

多头潜在注意力（MLA）由 DeepSeek-V2 提出，它以一种与 MQA/GQA 不同的方式来减少 KV 缓存。它并不主要依靠共享 K/V 头，而是对键和值信息使用低秩联合压缩。[11]

按照原论文的记号，一个压缩后的潜在表示被构造为：

$$
c_t^{KV} = W^{DKV} h_t,
$$

之后键/值内容可以通过学习到的上投影（up-projection）来恢复。其核心动机在于，被缓存的状态可以远小于存储常规的逐头键和值。

不过，一个准确的描述需要加上一个重要的补充说明：DeepSeek-V2 同时使用了解耦的 RoPE。与位置相关的键分量（源自 RoPE）被单独处理，因为直接在压缩后的键路径中应用 RoPE，会妨碍用于高效推理的一部分投影吸收（projection absorption）技术。因此，论文中报告的缓存实际上同时包含压缩后的潜在向量和一个解耦的位置键分量，而并非在完整实现中真的只有一个裸的潜在向量。[11]

| 机制 | KV 缓存核心思路 | KV 头数量 |
|---|---|---|
| MHA | 存储逐头的 K 和 V | 多 |
| MQA | 共享一个 K/V 头 | 一 |
| GQA | 在组内共享 K/V | 介于两者之间 |
| MLA | 低秩联合 KV 压缩（+位置分量）| 一种不同的表述方式 |

> **重要提示。** MLA 并不只是"头更少的 GQA"。它改变的是被缓存的表示本身，以及推理过程中所使用的投影代数。

---

## 20 究竟什么是"二次方"的？

"注意力是二次方的"这句话其实压缩了几个不同的资源问题。

对于序列长度为 $n$、且表示/头维度保持固定的稠密自注意力，原始 Transformer 的分析给出了每层自注意力项为 $O(n^2 d)$。[3]

但有三个不同的量需要区分开来：

1. **算术工作量。** 计算所有稠密的查询-键交互并将其作用于值，这一过程包含一个随序列长度呈二次方增长的项。
2. **实际存储的中间内存。** 一种朴素的实现可能会显式地存储整个 $n \times n$ 的分数/概率矩阵，从而带来二次方的中间存储量。
3. **内存流量。** 在 GPU 不同层级的内存之间读写这些中间结果，即便算术运算量不变，也可能主导运行时间。

这些区分解释了为什么以下两个说法可以同时成立：

- 稠密注意力确实执行的是二次方的成对交互。
- 一个精确的注意力实现可以避免把整个二次方的注意力矩阵完整地放入高带宽内存，从而运行得快得多。

第二点正是 FlashAttention 的核心所在。

**心智模型。** 复杂度、内存占用、内存流量和实际运行时间彼此相关，但并不能互相替代。永远不要把其中一个当作另一个的同义词来使用。

---

## 21 FlashAttention：相同的注意力，不同的执行方式

FlashAttention 是一种"IO 感知"的精确注意力算法。它的核心洞见是显式地考虑 GPU 的内存层级结构：高带宽内存（HBM）与更快的片上 SRAM 之间的数据传输，往往会成为主要的开销来源。FlashAttention 不再把完整的注意力矩阵存放在 HBM 中，而是把计算切分成小块（tile），使得 Q/K/V 的分块以及中间统计量可以在片上处理，从而减少 HBM 的读写次数。[8]

其数学目标依然是缩放点积注意力。该算法使用一种在线/分块的 softmax 策略，使得精确的结果可以在不一次性把所有分数都存入 HBM 的情况下被组装出来。

**流程：** HBM（存放 Q、K、V、输出）↔ 片上 SRAM/寄存器（存放分块）→ 矩阵乘法 + softmax 更新 → 更少的大规模内存传输

原论文强调的重点是"减少 HBM 访问次数"，而不是"用某种近似替代稠密注意力"。斯坦福 CS336 课程正是把 FlashAttention 当作一个系统性练习，用来把 GPU 内存层级结构与注意力性能联系起来。[8, 16]

> **重要提示。** FlashAttention 并不会把标准稠密注意力的算术复杂度从 $O(n^2)$ 变成 $O(n)$。它减少的是内存流量，并且在计算精确的稠密注意力时，避免了对注意力矩阵进行二次方级别的 HBM 物化（materialization）。

---

## 22 FlashAttention 的迭代：优化你真正遇到的瓶颈

其持久不变的核心概念是"IO 感知的精确注意力"。后续版本的 FlashAttention 不断细化这一计算如何映射到更新的 GPU 硬件之上。

FlashAttention-2 改进了并行度和工作划分方式，包括在线程块和线程束（warp）之间更好地分配工作，以及减少非矩阵乘法运算。[10]

FlashAttention-4 于 2026 年针对 Blackwell 级别的 GPU 提出，针对的是一种不同的硬件平衡问题：张量核心的吞吐量增长速度已经超过了周围一些资源，因此瓶颈转移到了 softmax/非矩阵乘法运算、共享内存流量以及流水线结构上。它的贡献并不是重新定义注意力，而是针对已经变化的硬件所做的算法/内核协同设计。[12]

这是一条重要的工程经验：

**心智模型。** 一个算法在数学上可以保持不变，但其最快的实现方式会随硬件变化而变化。"注意力"是一种数学运算；"FlashAttention-N"则是一系列在硬件感知程度上不断提升的、执行这一运算的方式。

作为一种历久弥新的心智模型，请记住：

1. 避免不必要的物化（materialization）；
2. 对数据分块以利用更快的内存；
3. 融合可以兼容的运算；
4. 让昂贵的计算单元始终保持忙碌；
5. 当硬件比例发生变化时，重新审视瓶颈所在。

本书有意省略了各个 FlashAttention 版本的具体基准测试数字，因为它们高度依赖具体硬件、张量形状、精度和软件版本。

---

## 23 稀疏注意力与局部注意力

稠密注意力把每个查询与每个键都连接起来，对于等长自注意力而言会产生 $O(n^2)$ 的成对交互。另一类方法则改变了连接模式本身。

Longformer 把局部窗口注意力与面向任务的全局注意力结合起来，其设计使得在固定窗口模式下，注意力的开销随序列长度线性增长。[5]

BigBird 结合了局部、全局和随机连接等稀疏成分，同样降低了完全二次方的依赖关系，同时在其设计下保留了重要的理论性质。[6]

这些方法在概念上与 FlashAttention 不同：

| 方法 | 改变了什么？ | 是否是精确的完整稠密注意力？ |
|---|---|---|
| FlashAttention | 执行方式 / IO 策略 | 是 |
| 局部/稀疏注意力 | 哪些查询-键对被计算 | 否，连接性受到限制/结构化 |

稀疏注意力可以通过不计算所有的成对交互来减少算术工作量。其代价是模型在单层中不再具有无限制的直接连接性；连接模式的设计因此变得重要。

> **重要提示。**"高效注意力"并不是单一的一种技术。有些方法保留了精确的稠密注意力、只是执行得更好；另一些方法则改变了注意力的模式或使用近似，以减少交互的数量。

---

## 24 一个透明的 PyTorch 实现

以下代码在一个简单的单头/多头张量布局下，忠实地反映了上述数学过程。它有意写得直白，而非追求性能优化。

```python
import math
import torch

# q, k, v 的形状: (..., L/S, d_head)
scores = q @ k.transpose(-2, -1)
scores = scores / math.sqrt(q.size(-1))

if causal:
    L, S = q.size(-2), k.size(-2)
    # 这个简单的掩码假设的是对齐的方阵情形。
    mask = torch.triu(
        torch.ones(L, S, dtype=torch.bool, device=q.device),
        diagonal=1,
    )
    scores = scores.masked_fill(mask, float("-inf"))

weights = torch.softmax(scores, dim=-1)
out = weights @ v
```

这段代码展示了每一个确切的概念阶段：

$$
QK^\top \rightarrow \text{缩放} \rightarrow \text{掩码} \rightarrow \text{softmax} \rightarrow AV.
$$

它没有展示的是：一个生产级内核是如何融合各个运算的、如何处理数值稳定的分块 softmax、如何选择硬件后端、如何在非方阵情形下表示因果偏置，以及如何支持分组查询头。

> **重要提示。** 这里展示的简单三角掩码只适用于对齐的方阵因果情形。对于真实的非方阵缓存解码场景，请使用那些明确记录了其因果对齐语义的库 API，而不要假设这个掩码是普遍正确的。

---

## 25 PyTorch 中的生产级 SDPA

当前的 PyTorch 将缩放点积注意力暴露为：

```python
torch.nn.functional.scaled_dot_product_attention(
    query, key, value,
    attn_mask=None,
    dropout_p=0.0,
    is_causal=False,
    scale=None,
    enable_gqa=False,
)
```

该文档定义了查询长度 $L$、源/键长度 $S$、可选的布尔型或加性掩码、默认缩放因子 $1/\sqrt{d}$、因果偏置行为、dropout，以及实验性的分组查询支持。根据输入和后端的不同，PyTorch 可能会调度到经过优化的实现，而不是真的按字面顺序执行上面那段教学性质的 Python 代码。[17]

以下是几点实现方面的经验教训：

- **API 语义很重要：** 掩码在不同库之间并不能互换使用。在当前的 PyTorch SDPA 中，布尔型 `attn_mask` 中值为 `True` 表示"该位置参与注意力计算"；这与 `MultiheadAttention` 中布尔型 `key_padding_mask` 的约定恰好相反。
- 在训练和评估逻辑之间切换时，必须显式地控制 dropout 行为。
- 在当前的实现中，GQA 存在形状/可整除性方面的约束。
- 内核的选择可能取决于设备、数据类型、张量形状、掩码以及特性支持情况。

实现相关的 API 会不断演进。本手册中的数学内容是历久不变的；但生产代码应当始终对照所安装框架版本的文档进行核实。

---

## 26 常见的实现错误

**1. Softmax 用错了维度**

注意力应当对每个查询沿着键的维度进行归一化。如果沿查询行方向做 softmax，运算的性质就变了。

**2. 忘记缩放**

未经缩放的点积注意力并不是 Transformer 所定义的形式，并且随着头维度变化，其行为也可能不同。

**3. 在 softmax 之后才做掩码**

在 softmax 之后把概率直接置零、却不重新归一化，这通常并不等价于在归一化之前就排除这些 logits。

**4. 在 $L \neq S$ 的情况下假设是方阵掩码**

缓存解码和交叉注意力常常具有不同的查询/键长度。因果对齐方式必须与所用框架的约定相匹配。

**5. 混淆头数与 KV 头数**

在 GQA 中，查询头和键/值头的数量是不同的。涉及形状的逻辑以及缓存计算公式，都必须使用 $H_{kv}$ 来计算 K/V 存储量。

**6. 把注意力权重当作该层的输出**

权重只是一个中间量。真正的输出是 $AV$（在多头注意力中，随后还会经过拼接/输出投影，以及周围 Transformer 模块的其他运算）。

**7. 比较"注意力内存占用"时没有明确到底在计算什么**

权重、临时的分数矩阵、KV 缓存、激活值以及分配器开销，这些都是不同的量。

**心智模型。** 大多数注意力方面的 bug 都是关于形状/维度轴/语义的错误，而不是记不清主公式。要明确地追踪每一个维度。

---

## 27 常见的误解

| 误解 | 更正 |
|---|---|
|"Q 是问题，K 是答案。"| Q/K 是学习到的向量投影，它们的点积提供的是兼容性信号。这个类比并不是真正的机制。|
|"注意力就是相似度检索。"| 点积是学习到的、面向特定任务的兼容性分数；之后值才被聚合起来。|
|"注意力权重就是输出。"| 它们只是系数；输出是 V 的加权和。|
|"自注意力就是因果注意力。"| 自注意力说的是 Q/K/V 来自同一个流；因果性则是一种掩码/连接性约束。|
|"FlashAttention 是线性注意力。"| FlashAttention 计算的是精确的稠密注意力，只是采用了一种 IO 感知的执行策略。|
|"KV 缓存让注意力变成常数时间。"| 它复用的是过去的 K/V，但一个新的稠密查询仍然要与不断增长的前缀进行交互。|
|"MQA/GQA 减少的是查询头。"| 它们真正改变的是相对于查询头而言，KV 头的共享/减少程度。|
|"RoPE 能带来无限的上下文。"| 位置表示本身并不能保证经过训练后就具备长上下文的能力。|

一个好的技术讲解应当尽早消除这些捷径式的说法，因为后续的系统性概念都直接建立在这些区分之上。

---

## 28 注意力权重是解释吗？

注意力权重往往在视觉上很有说服力：它们构成了输入位置上的一个分布，因此很容易让人觉得"权重高就说明模型是因为用到了这个词元才如此"。但这一结论如果没有额外的分析支撑，通常是站不住脚的。

Jain 和 Wallace 在多个 NLP 模型上测试了"把注意力权重当作解释"这一做法，发现在一些情况下，学习到的注意力与基于梯度的重要性度量之间的关联很弱，并且非常不同的注意力分布也可能产生相似的预测结果。他们主张，标准的注意力权重不应当被自动地当作忠实的解释。[13]

Wiegreffe 和 Pinter 则对这种一概而论的否定提出了质疑，认为答案取决于"解释"的定义以及所采用的基线/检验方法。他们提出了一些诊断性条件，在满足这些条件的情况下注意力仍然可以提供信息。[14]

因此，最稳妥的技术性结论要比两种极端说法都更加谨慎：

**心智模型。** 注意力权重是模型内部的一种量，有时可以用来辅助分析，但它们并不会自动成为对模型预测的、忠实的因果解释。可解释性方面的论断需要有相应的方法和验证，而不能仅仅依靠画出权重图。

这一细微差别在现代 Transformer 中尤为重要，因为最终的输出是由许多头、许多层、残差通路、前馈模块以及后续计算共同作用产生的。

---

## 29 完整的心智模型

把注意力机制记忆为六个步骤是最容易的方式。

1. **投影（Project）。** 隐藏状态被映射为学习到的查询、键和值表示。
2. **比较（Compare）。** 每个查询与被允许的键进行比较，通常通过点积实现。
3. **缩放（Scale）。** 在缩放点积注意力中，点积要除以 $\sqrt{d_k}$。
4. **约束（Constrain）。** 掩码或偏置项定义了哪些交互是被允许或被偏好的。
5. **归一化（Normalize）。** 按行的 softmax 把分数转化为在各个键上的权重。
6. **聚合（Aggregate）。** 这些权重把对应的值混合进查询的新表示之中。

本书其余的一切内容，都是在修改这六个步骤周围的某个维度：

- 多头注意力在多个学习到的子空间中重复这一运算；
- RoPE/位置方法让注意力具备位置感知能力；
- 因果掩码强制执行自回归的信息流动；
- KV 缓存在增量解码过程中复用过去的 K/V；
- MQA/GQA 减少 KV 头的数量，从而降低缓存/带宽成本；
- MLA 用一种不同的投影策略来压缩被缓存的 KV 表示；
- FlashAttention 以更低的内存流量成本执行精确的稠密注意力；
- 稀疏/局部注意力改变了要计算哪些交互对，以降低稠密连接的成本。

如果你已经清楚这些区分，那么你所掌握的就不仅仅是注意力公式本身。你已经具备了理解现代大语言模型架构与推理系统为何持续在其周围不断演变的概念基础。

---

## 参考文献与延伸阅读

**参考文献**

[1] Dzmitry Bahdanau, Kyunghyun Cho, Yoshua Bengio. *Neural Machine Translation by Jointly Learning to Align and Translate*. arXiv:1409.0473; ICLR 2015.

[2] Minh-Thang Luong, Hieu Pham, Christopher D. Manning. *Effective Approaches to Attention-based Neural Machine Translation*. EMNLP 2015. arXiv:1508.04025.

[3] Ashish Vaswani et al. *Attention Is All You Need*. NeurIPS 2017. arXiv:1706.03762.

[4] Noam Shazeer. *Fast Transformer Decoding: One Write-Head is All You Need*. arXiv:1911.02150, 2019.

[5] Iz Beltagy, Matthew E. Peters, Arman Cohan. *Longformer: The Long-Document Transformer*. arXiv:2004.05150, 2020.

[6] Manzil Zaheer et al. *Big Bird: Transformers for Longer Sequences*. NeurIPS 2020. arXiv:2007.14062.

[7] Jianlin Su et al. *RoFormer: Enhanced Transformer with Rotary Position Embedding*. arXiv:2104.09864, 2021.

[8] Tri Dao, Daniel Y. Fu, Stefano Ermon, Atri Rudra, Christopher Re. *FlashAttention: Fast and Memory-Efficient Exact Attention with IO-Awareness*. NeurIPS 2022. arXiv:2205.14135.

[9] Joshua Ainslie et al. *GQA: Training Generalized Multi-Query Transformer Models from Multi-Head Checkpoints*. EMNLP 2023. arXiv:2305.13245.

[10] Tri Dao. *FlashAttention-2: Faster Attention with Better Parallelism and Work Partitioning*. arXiv:2307.08691, 2023.

[11] DeepSeek-AI et al. *DeepSeek-V2: A Strong, Economical, and Efficient Mixture-of-Experts Language Model*. arXiv:2405.04434, 2024.

[12] Ted Zadouri et al. *FlashAttention-4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling*. arXiv:2603.05451, 2026.

[13] Sarthak Jain, Byron C. Wallace. *Attention is not Explanation*. NAACL-HLT 2019.

[14] Sarah Wiegreffe, Yuval Pinter. *Attention is not not Explanation*. EMNLP-IJCNLP 2019.

[15] Stanford CS224N. *Natural Language Processing with Deep Learning*, Winter 2026 materials; Assignment 3: Self-Attention and Transformers.

[16] Stanford CS336. *Language Modeling from Scratch*, current course materials; systems assignment includes a Triton FlashAttention-2 implementation.

[17] PyTorch documentation. `torch.nn.functional.scaled_dot_product_attention`. Current documentation consulted for API shapes, masks, causality, scaling, dropout, and GQA behavior.

*研究说明：书中的公式、张量形状论断、手算数值示例以及与具体实现相关的论断，均在成稿过程中经过独立核对。具名方法均以原始论文为依据；当前框架的具体行为应始终对照所安装的版本重新核实。*


